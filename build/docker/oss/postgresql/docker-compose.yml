version: "3"

services:
  postgres:
    container_name: postgres
    image: postgres
    tty: true
    environment:
      POSTGRES_DB: blog
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1234
    ports:
      - 5432:5432
